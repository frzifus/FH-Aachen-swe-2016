package swe.game;

import com.badlogic.gdx.math.Intersector;

/**
 * @author Frzifus
 */

public class Collision {
  // test
  // void stuff() {
  //   if(Intersector.overlaps(pongPlayerOne.getpSprite().getBoundingRectangle(),
  //                           pongSphere.getpSprite().getBoundingRectangle()) ||
  //      Intersector.overlaps(pongPlayerTwo.getpSprite().getBoundingRectangle(),
  //                           pongSphere.getpSprite().getBoundingRectangle())
  //      ) {
  //     // DoSomething
  //    }
  // }
}
